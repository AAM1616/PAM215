import { Text, StyleSheet, View } from 'react-native'

export default function FlatlistScreen() {
    return (
      <View>
        <Text>Proximamente por Diego E...</Text>
      </View>
    )
  }


const styles = StyleSheet.create({})